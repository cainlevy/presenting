<% form_for @form.presentable, :url => @form.url, :method => @form.method do |f| %>
  <% @form.fields.each do |field| %>
    <fieldset>
      <%= f.label field %>
      <%= f.text_field field %>
    </fieldset>
  <% end %>
  <%= f.submit 'Save' %>
<% end %>
